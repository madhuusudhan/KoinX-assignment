{
  "name": "your-project-name",
  "version": "1.0.0",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "nodemon server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^your_version",
    "mongoose": "^your_version",
    "dotenv": "^your_version",
    "cron": "^your_version"
  },
  "devDependencies": {
    "nodemon": "^your_version"
  }
}
